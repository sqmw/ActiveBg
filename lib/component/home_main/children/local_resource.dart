import 'package:flutter/material.dart';

class LocalResource extends StatelessWidget {
  const LocalResource({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TextButton(
      onPressed:(){

      },
      child: const Text("本地视频/壁纸"),
    );
  }
}
